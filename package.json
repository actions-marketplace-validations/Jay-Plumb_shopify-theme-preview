{
  "config": {
    "store": "auto-preview"
  },
  "name": "shopify-theme-preview",
  "version": "1.0.0",
  "description": "Shopify theme preview",
  "scripts": {
    "shopify:serve": "shopify theme dev --store=$npm_package_config_store",
    "dev": "run-p shopify:serve"
  },
  "author": "Jay Plumb",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
